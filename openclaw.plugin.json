{
  "id": "openclaw-qq",
  "name": "OpenClaw QQ Plugin",
  "version": "0.1.0",
  "description": "QQ channel plugin for OpenClaw via NapCat (OneBot v11)",
  "author": "CreatorAris",
  "license": "MIT",
  "configSchema": {
    "type": "object",
    "properties": {
      "napcatWs": {
        "type": "string",
        "description": "NapCat OneBot v11 WebSocket URL (e.g. ws://127.0.0.1:3001)"
      },
      "napcatToken": {
        "type": "string",
        "description": "NapCat access_token for WebSocket authentication"
      },
      "botQQ": {
        "type": "string",
        "description": "Bot's QQ number, used to filter self-messages in groups"
      },
      "allowedUsers": {
        "type": "array",
        "items": { "type": "string" },
        "description": "Allowed QQ user IDs for private chat. Empty array = allow all."
      },
      "allowedGroups": {
        "type": "array",
        "items": { "type": "string" },
        "description": "Allowed group IDs for group chat. Empty array = disabled."
      },
      "port": {
        "type": "number",
        "description": "HTTP port for proactive /send endpoint (0 = disabled)",
        "default": 0
      },
      "voice": {
        "type": "object",
        "description": "Voice message configuration",
        "properties": {
          "enabled": {
            "type": "boolean",
            "description": "Enable voice message recognition (SILK → WAV → STT). Requires python3 + pilk.",
            "default": true
          },
          "ttsToolPath": {
            "type": "string",
            "description": "Path to TTS tool script (node script accepting: tts <text> <output.wav>). Required for voice reply."
          },
          "qqDataDir": {
            "type": "string",
            "description": "QQ data directory for resolving bare voice filenames. Defaults to macOS QQ container path."
          },
          "cacheDir": {
            "type": "string",
            "description": "Voice cache directory. Must be inside QQ's sandbox on macOS to avoid EPERM. Defaults to QQ container tmp dir."
          }
        }
      }
    }
  }
}
